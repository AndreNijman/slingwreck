Authorised: change `tools/playtest.mjs`.

You were right to stop. The sweep samples tap steps only up to `impactStep - 1`, so the
rest-phase window that was just added for `boom` and `inflate` is never measured, and
Lob's `2/46` is still the old limitation rather than a result. Respecting the file scope
and saying so was the correct call.

## What to change

Extend the sweep past impact for any ability whose params declare `tappableAtRest`:

- Continue sampling into the `settling` phase, up to the point the round returns to
  `aiming` or the critter is culled, whichever comes first.
- For abilities without `tappableAtRest`, keep the existing behaviour — sampling past
  impact there would only add rows where `tap()` correctly refuses.
- Report the winning-timings denominator honestly per critter, since it now differs
  between abilities. `2/46` and `2/120` mean very different things and the table must not
  blur them.
- Mark in the output which winning timings fall **after** impact, so it is visible at a
  glance whether Lob is being saved by the rest window or by the frame before contact.

## Then answer the question that started this

Report, for Lob:

- winning timings before this change and after,
- the untapped baseline,
- the best tap score and whether the best tap is now before or after impact,
- and how many of the winning timings are in the rest window.

Then state plainly whether the fuse is too generous. The test is whether a tap
meaningfully beats simply letting the fuse fire. If it does not — if the fuse is as good
as any tap — then the fuse is the ceiling rather than the floor and `fuseSeconds` should
go up. **Say that if it is what the numbers show.** Do not adjust `fuseSeconds` in this
task; report and let me decide.

Also re-report the full winning-timings table for all nine critters with the corrected
denominators, since Hulk's numbers change too.

## Acceptance

1. `node tools/playtest.mjs --all` and `node tools/playtest.mjs --sweep --no-shots` pass.
2. `node tools/check.mjs`, `npm test`, `npm run test:audio` still pass.
3. The reported denominators differ between rest-tappable and flight-only abilities, and
   the table says which.

Write only `tools/playtest.mjs`.
